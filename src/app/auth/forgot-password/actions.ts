"use server";

export async function adminResetPassword(_email: string, _newPassword: string) {
  // Password reset flow will be implemented via Express API
  return { error: "Password reset is not yet available." };
}
